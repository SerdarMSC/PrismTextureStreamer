name: Build DLL (Windows)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    strategy:
      matrix:
        configuration: [Release]
        platform: [x64, Win32]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup MSBuild (add msbuild to PATH)
        uses: microsoft/setup-msbuild@v1

      - name: Show environment info
        shell: cmd
        run: |
          echo Configuration=${{ matrix.configuration }} Platform=${{ matrix.platform }}
          echo WORKING DIR: %CD%
          where msbuild || echo "msbuild not found"
          msbuild /version || echo "msbuild /version failed"
          echo ==== dir listing repo root ====
          dir /b

      - name: Skip NuGet restore for native C++ projects
        shell: cmd
        run: |
          echo "No NuGet restore needed for native C++ projects"

      - name: Build solution with MSBuild (force SDK 10.0.19041.0)
        shell: cmd
        run: |
          echo Forcing WindowsTargetPlatformVersion to 10.0.19041.0 to avoid SDK detection issues on hosted runners
          echo Building with Configuration=${{ matrix.configuration }} Platform=${{ matrix.platform }}
          msbuild PrismTextureStreamerFB.sln /p:WindowsTargetPlatformVersion=10.0.19041.0 /p:Configuration=${{ matrix.configuration }} /p:Platform=${{ matrix.platform }} /p:OutDir=%CD%\artifacts\ /verbosity:normal /m
          if %ERRORLEVEL% neq 0 (
            echo MSBuild failed with %ERRORLEVEL%
            exit /b %ERRORLEVEL%
          )

      - name: Show artifacts dir
        shell: cmd
        run: |
          echo ==== artifacts content ====
          if exist artifacts (
            dir artifacts /s /b
          ) else (
            echo "artifacts folder not found"
          )

      - name: Upload DLL artifacts
        uses: actions/upload-artifact@v4
        with:
          name: prismtexturestreamer-dlls
          path: artifacts/**/*.dll
